version: '3'
services:
  executor-php:
    container_name: ohmycode-executor-php82
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./requests:/app/requests
      - ./results:/app/results
